--- supabase/functions/request-password-reset/index.ts
+++ supabase/functions/request-password-reset/index.ts
@@ -37,6 +37,7 @@
     .replace(/'/g, "&#039;");
 }
 function getEmailHtml(displayName: string, resetLink: string): string {
+  const safeDisplayName = displayName ? escapeHtml(displayName) : "";
   return `
 <!DOCTYPE html>
 <html lang="es">
@@ -69,7 +70,7 @@

                     <!-- Greeting -->
                     <p style="color: #3D3530; font-size: 18px; font-weight: 500; margin: 0 0 8px 0;">
-                      Hola${displayName ? `, ${displayName}` : ""}
+                      Hola${safeDisplayName ? `, ${safeDisplayName}` : ""}
                     </p>

                     <!-- Message -->
